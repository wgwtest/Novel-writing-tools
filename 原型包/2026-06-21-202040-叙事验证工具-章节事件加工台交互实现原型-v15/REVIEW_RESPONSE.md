# V15 评审响应

## 输入意见

用户要求：基于上一轮章节事件加工台效果图，先实现一版。

## 本版处理

1. 保留 V14 的三段式结构：
   - 左侧输入事件池。
   - 中部章节内部加工台。
   - 右侧输出事件池。
2. 新增点击选择逻辑：
   - 单击对象只选中。
   - 不直接跳转详情页。
   - 通过按钮再进入对应视图。
3. 新增 Inspector：
   - 说明选中对象的类型、归属、作用和风险。
   - `#logic` 与 `#check` 使用右侧 Inspector。
   - `#overview` 使用底部选中对象摘要，避免破坏右侧输出池。
4. 新增相关对象高亮：
   - 当前对象为 `selected`。
   - 相关对象为 `related`。
   - 无关对象为 `dimmed`。

## 明确未处理

- 没有修改全局时间线。
- 没有实现真实事件详情页。
- 没有实现持久化数据。
- 没有把事件图谱合并进章节加工台。

## 待用户判断

1. `overview` 页底部摘要是否足够，还是需要独立 Inspector 区。
2. 相关对象高亮是否会制造过多视觉噪声。
3. 后续是否需要把 `objectModel` 抽成独立 JSON，以便继续扩展真实数据结构。
